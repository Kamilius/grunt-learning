/*! grunt-tutorial 2014-10-06 */
function elementInViewport(a) {
    var b = a.offsetTop;
    var c = a.offsetLeft;
    var d = a.offsetWidth;
    var e = a.offsetHeight;
    while (a.offsetParent) {
        a = a.offsetParent;
        b += a.offsetTop;
        c += a.offsetLeft;
    }
    return b < window.pageYOffset + window.innerHeight && c < window.pageXOffset + window.innerWidth && b + e > window.pageYOffset && c + d > window.pageXOffset;
}

$(document).ready(function() {
    var a = $(window);
    $(".slide").each(function(b) {
        var c = $(this);
        $(window).scroll(function() {
            if (elementInViewport(c.get(0))) {
                var b = (c.offset().top - a.scrollTop()) / c.data("speed"), d = "50% " + b + "px";
                c.css({
                    backgroundPosition: d
                });
            }
        });
    });
});